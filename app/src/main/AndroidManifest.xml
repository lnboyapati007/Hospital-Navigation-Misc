<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.hospital_navigation_system">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Hospital Navigation System"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hospital_Navigation_System"
        tools:targetApi="31" >

        <activity android:name=".navigation_codes.Navigation"></activity>
        <activity android:name=".TestingVertex" />
        <activity android:name=".navigation_cafetaria" />
        <activity android:name=".navigation_pharmacy" />
        <activity android:name=".navigation_restrooms" />
        <activity android:name=".MiscActivity" />

        <activity android:name=".navigation_codes.showNavigation" />
        <activity android:name=".navigation_main2" />

        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>